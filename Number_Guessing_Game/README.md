# Number_Guessing_Game
First Project: Number Guessing Game

# What The Game Does:

*This is a textual basic gaming project. The basic concept of this game as the name suggest is about guessing a random number generated by the computer.

*The user is prompted to guess the number and is accordingly prompted for higher or lower number until the user eventually guesses the number.

*It also counts the number of valid attempts that the user has taken before guessing the actual number.

*User are also given an option of playing again after completing the game once.

*I have 3 different difficulty level to keep up excitement.

Language Used: 

*C Language Used

Code Editor:

*VS Code

# Concepts used:

*Variables

*Data Types

*Conditions

*Loops

*Random Module(AI Assistance)

*Functions

*Libraries: math.h;  stdio.h; math.h; stdlib.h

# Features:

*Random Number Generator

*Attempt Counter

*Play Again Option

*Difficulty Levels

# How to run main.c: 

Prerequistes:
Ensure you have a C compiler installed(like 'gcc').
* **Linux/Ubuntu:** `sudo apt install build-essential`

* **macOS:** `xcode-select --install`

* **Windows:** Use [MinGW](https://www.mingw-w64.org/) or WSL.

# Compilation & Execution

1. **Compile the code:**

*Open your terminal or command prompt and navigate to the folder containing main.c.

*Compile the code using GCC:
   gcc main.c -o main

2. **Execution:**

*Linus/MacOS
./main

*Windows
main.exe

Shortcut:

*If you have VS Code installed just copy and paste main.c after and run it as usual. 

Problems to handle:

1. When taking user inputs at any place, I should have dealt with the unexpected inputs.

2. When taking user input for guessed number, I should have prompted the user for number in the range and probably should have counted it as invalid attempt.

P.S. :- Well, Although these are really some problems. But thinking about the main prototype, this is a super simple game. We just do three things mainly- Generate random, Let user guess the random and then prompt him. Other things in the code are mostly ornamental. There is no complex algorithm or optimisation problem. 

cough...cough.... although I hardcorded a bit which is easily resolvable using functions but I am very lazy these days and won't be using my time for this. Sleeping takes priority.

The problems are unsolvable by me currently because although i could deal with some kind of input. There are many other that can't be dealt by me alone.